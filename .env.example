# Copy this file to `.env.local` and fill in the values.

# AI Assistant — Google Gemini API key (server-side only, never exposed to the browser).
# Free key: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=
