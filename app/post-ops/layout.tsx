import { DashboardLayout } from "@/components/dashboard-layout"

export default function PostOpsLayout({ children }: { children: React.ReactNode }) {
    return <DashboardLayout>{children}</DashboardLayout>
}
