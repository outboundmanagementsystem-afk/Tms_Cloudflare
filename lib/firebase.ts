// Firebase replaced by Cloudflare D1 + R2.
// This stub keeps any remaining import references from breaking at build time.
// Components that still import `storage` should be migrated to use /api/storage/* instead.

export const auth = null
export const db = null
export const storage = null
export const googleProvider = null
